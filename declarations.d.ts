// Plain CSS global imports (e.g. import "./globals.css") are side-effect only.
// Next.js only declares *.module.css in its built-in types, so we declare
// bare *.css here so VS Code's TS language server resolves them cleanly.
declare module '*.css';
